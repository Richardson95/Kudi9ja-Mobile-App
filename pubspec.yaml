name: kudi9ja
description: "Kudi9ja — Pay smart. Pay global. Microlending & high-yield savings."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  google_fonts: ^6.2.1
  intl: ^0.20.2
  fl_chart: ^0.69.2
  flutter_animate: ^4.5.0
  local_auth: ^2.3.0
  uuid: ^4.5.1
  crypto: ^3.0.7
  image_picker: ^1.2.3
  # A bank statement is whatever the bank sent — usually a PDF, sometimes a
  # spreadsheet or a Word file. image_picker cannot see any of those, so asking
  # for a statement with it means asking somebody to photograph their own
  # screen to get past us.
  file_picker: ^8.1.7
  # Talking to the Kudi9ja server.
  http: ^1.2.2
  http_parser: ^4.0.2
  # Tokens go in the platform keystore, not shared_preferences: a refresh token
  # is a bearer credential worth thirty days of access.
  flutter_secure_storage: ^9.2.2
  # Receiving push. The server already sends through Firebase whenever money
  # moves; these are what let a handset hear it.
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.5
  # Firebase draws a notification itself only when the app is in the background.
  # In the foreground the message is handed to the app and nothing is shown, so
  # a customer watching the screen was the one person who did not find out their
  # money had landed. This draws it. It also creates the high-importance channel
  # the manifest names — Android will not create it for us, and a message sent
  # to a channel that does not exist arrives at default importance, silently.
  flutter_local_notifications: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.1
  flutter_native_splash: ^2.4.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/app_icon.png"
  adaptive_icon_background: "#000000"
  adaptive_icon_foreground: "assets/icons/app_icon_foreground.png"
  remove_alpha_ios: true

flutter_native_splash:
  color: "#000000"
  image: assets/icons/app_icon_foreground.png
  android_12:
    color: "#000000"
    image: assets/icons/app_icon_foreground.png
    icon_background_color: "#000000"
